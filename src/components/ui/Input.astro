---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"input">;

const { class: className, ...props } = Astro.props;
---

<input
  {...props}
  class={cn(
    "neobrutal p-2 outline-none focus:shadow-none transition-shadow",
    className,
  )}
/>
