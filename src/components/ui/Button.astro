---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"button">;

const { class: className, ...props } = Astro.props;
---

<button
  {...props}
  class={cn("neobrutal bg-accent p-4 m-4 cursor-pointer", className)}
  ><slot /></button
>
