---
import MouseSketches from "@/components/MouseSketches.astro";
import Socials from "@/components/Socials.astro";
import Typewriter from "@/components/Typewriter.svelte";
import ArtLayout from "@/layouts/ArtLayout.astro";
---

<ArtLayout title="sprucepad (arte)" desc="O lado criativo de sprucepad.net!">
  <section class="relative grid h-[85svh] place-content-center gap-4 p-4">
    <div class="absolute inset-0 -z-50">
      <MouseSketches />
    </div>

    <h1 class="text-center font-heading text-5xl font-black">
      Bem-vindo(a) ao meu site!
    </h1>
    <h2 class="text-center text-3xl font-black">
      Eu sou um
      <Typewriter
        class="font-heading"
        words={["artista de pixel", "desenvolvedor de jogos"]}
        client:load
      />, do Brasil.
    </h2>
  </section>

  <Socials class="m-4 mx-auto w-fit rounded-lg neobrutal p-4" />

  <section class="bg-accent p-4 text-accent-foreground">
    <h1 class="text-center font-heading text-4xl">Minha Arte</h1>
  </section>
</ArtLayout>
